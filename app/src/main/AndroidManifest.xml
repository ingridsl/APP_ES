<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ingrid.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuPrincipalActivity" />
        <activity android:name=".TipoActivity" />
        <activity android:name=".UnicoActivity" />
        <activity android:name=".RecorrenteActivity" />
        <activity android:name=".PeriodicoActivity" />
        <activity android:name=".ConfActivity" />
        <activity android:name=".HojeActivity" />
        <activity android:name=".TudoActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".EstatisticaActivity" />
        <activity android:name=".CalendarioActivity"></activity>
    </application>

</manifest>